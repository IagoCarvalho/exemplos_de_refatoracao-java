class cliente {
  Date newStart = new Date (previousEnd);

  private static Date nextDay(Date previousEnd){
    return previousEnd.getYear(),
     previousEnd.getMonth(), previousEnd.getDate() + 1;
  }
}
